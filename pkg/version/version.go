// Package version provides the sporemind version. The canonical value is
// injected at link time via -ldflags -X, with a runtime fallback to the
// SPOREMIND_VERSION environment variable or the version file at the repo root.
package version
