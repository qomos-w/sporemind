// Lightweight types for the generated third-party notices asset.
// Local asset interface (not a frontend protocol type).
import type { LicenseEntry, LicenseGroup, ThirdPartyNotices } from '../../scripts/generate-third-party-notices'

export type { LicenseEntry, LicenseGroup, ThirdPartyNotices }
