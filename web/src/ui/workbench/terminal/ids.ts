/**
 * The workbench terminal card registry id. Kept in a leaf module so pure
 * helpers (e.g. projectionBoard) can reference it without pulling the terminal
 * panel / shell client dependency graph.
 */
export const TERMINAL_CARD_ID = 'terminal'
